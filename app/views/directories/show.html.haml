- # app/views/directories/show.html.haml

- presenter = DirectoryPresenter.new @current_directory

%h2
  = presenter.label
  %small This is a Test of the Emergency Broadcast System. This is Only a Test.

%p
  Current path:
  = params[:directories] || "None"

%p
  Current directories:
%ul
  - if @directories.blank?
    %li None
  - @directories.each do |directory|
    %li= directory.slug
